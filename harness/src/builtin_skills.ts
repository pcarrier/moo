import type { BuiltinSkill } from "./types";

// Harness-bundled skills are compiled into the TypeScript harness and synced
// into the same pointer/object-backed store as user-created skills. Add entries
// here when a skill should ship with the harness itself.
export const BUILTIN_SKILLS: BuiltinSkill[] = [
  {
    id: "workflows",
    name: "Workflows",
    content: "---\ndescription: >\n  Use when designing, authoring, implementing, inspecting, executing, or debugging Moo workflows.\n  Covers compact Moo-shaped workflow DSLs, blob-backed JSON IR, pointers/RDF storage,\n  durable run execution, workflow views, dynamic Mermaid execution diagrams, chat links, and workflow APIs.\n---\n\n# Moo workflows\n\nUse this skill when work involves Moo workflow design, implementation, UI, storage, APIs, or docs.\n\n## Product model\n\n- Workflows are first-class objects with their own top-level view.\n- A workflow definition is not a chat. A workflow run is not a chat.\n- Chats may start, link to, steer, or receive notifications about workflow runs, but chats are optional control surfaces.\n- Workflows do not spawn chats. Do not add `chat.create`, spawned chat steps, or chat-owned execution state.\n- The run view is the canonical execution/debug UI; chat cards are compact projections.\n\n## Authoring model\n\nPrefer a compact Moo-shaped TypeScript/JavaScript DSL that emits declarative JSON IR:\n\n```ts\nexport default moo.workflow('linear', w => {\n  const i = w.input;\n  const s = w.state;\n\n  return w.flow(\n    w.step('linear.issue.get')\n      .mcp.linear.getIssue({ id: i.issue })\n      .out(s.ticket),\n\n    w.step('plan.approve')\n      .ui.ask({\n        spec: {\n          title: 'Approve plan',\n          fields: [\n            { id: 'ok', type: 'boolean', label: 'Approve?' },\n            { id: 'notes', type: 'textarea', label: 'Feedback' },\n          ],\n        },\n        context: { plan: s.plan },\n      })\n      .out(s.approval),\n\n    w.stopUnless(s.approval.ok, 'Plan rejected.'),\n    w.done({ summary: s.summary }),\n  );\n});\n```\n\nThe DSL is authoring sugar only. The harness runtime loads JSON IR; do not require a TypeScript compiler in the harness.\n\n## DSL rules\n\n- `moo.workflow(id, w => w.flow(...nodes))` defines a workflow.\n- `w.input.foo` and `w.state.foo` are refs, not runtime values.\n- Effects are explicit nodes: `w.step(id).mcp.server.tool(args)`, `agent.run`, `proc.run`, `ui.ask`.\n- Persist effect outputs with `.out(w.state.x)`.\n- Use declarative control nodes: `w.loop`, `w.when`, `w.break`, `w.goto`, `w.set`, `w.stopUnless`, `w.done`.\n- Do not use runtime JavaScript conditionals for state-dependent behavior; use `w.when(...)`.\n- Build-time composition helpers are fine if they emit static IR nodes.\n- Inputs are optional, immutable, and small. Use input for run identity/initial parameters; use state/artifacts for learned or produced context.\n\n## Storage model\n\nModel workflows with pointers plus RDF facts:\n\n- Store workflow IR as immutable JSON blobs/objects.\n- Point `workflow/<id>/current` at the current IR blob.\n- Cache decoded IR by blob hash, but treat the blob/pointer as canonical.\n- Store run input/output and large step payloads as blobs.\n- Store mutable run state, status, leases, and selected lightweight indexes in pointers.\n- Index queryable relationships in RDF: definitions, versions, runs, step cells, status, current step, waiting work, chat links, permissions, MCP/proc/agent usage.\n- Use pointer CAS/leases for interpreter ownership and resume safety.\n\n## Execution model\n\nA workflow run is a durable interpreter execution over JSON IR.\n\nFor each materialized step:\n\n1. resolve refs/expressions against input, state, and run context;\n2. if the step path already completed, reuse saved output;\n3. otherwise execute the effect;\n4. persist args, output/error, trace, timings, and status;\n5. write `out` refs into run state;\n6. continue or pause.\n\nLoops materialize paths such as `checks/check#1`, `checks/check.fix#1`, `checks/check#2`.\n\nHuman steps are run-owned waiting effects:\n\n- `ui.ask` pauses the run and appears in the run view plus global “Waiting on me”.\n- Submitting the form writes the step output, updates state, and resumes the interpreter.\n- The ask is not chat-owned.\n\n## UI model\n\nBuild a workflow debugger, not a visual workflow builder.\n\nTop-level Workflows view:\n\n- Library\n- Runs\n- Waiting on me\n- Failed\n- Scheduled\n\nRun view tabs:\n\n- Progress\n- State\n- Steps\n- Artifacts\n- Traces\n- Source\n\nRender a dynamic Mermaid execution diagram in the run view:\n\n- Generate Mermaid source from the current IR blob plus RDF step facts and run pointers.\n- Mermaid is a projection, never canonical state.\n- Color nodes by status: pending, running, waiting, done, failed, skipped.\n- Show materialized loop iterations and taken/skipped branches.\n- Clicking a node selects the step details panel.\n- Waiting `ui.ask` nodes render their form beside the diagram.\n\nChat integration:\n\n- Chat may call `start`, `linkChat`, `unlinkChat`, `inspectRun`, `submit`, `retry`, `fork`, or `cancel`.\n- Chat should show compact cards and meaningful transitions only; avoid step spam.\n- Compaction or archival of a chat must not affect run state.\n\n## Thin workflow API\n\nAgents and harness need small read APIs first:\n\n```ts\nmoo.workflows.list()\nmoo.workflows.inspect(id)\nmoo.workflows.runs({ status?, workflowId?, chatId? })\nmoo.workflows.inspectRun(runId)\nmoo.workflows.waiting({ chatId? })\n```\n\nControl APIs can follow:\n\n```ts\nmoo.workflows.start(id, { input?, workspaceId? })\nmoo.workflows.submit(runId, stepPath, value)\nmoo.workflows.retry(runId, opts?)\nmoo.workflows.fork(runId, opts?)\nmoo.workflows.cancel(runId)\nmoo.workflows.linkChat(runId, chatId)\nmoo.workflows.unlinkChat(runId, chatId)\n```\n\nKeep these APIs thin: list and inspect definitions/runs without exposing interpreter internals.\n",
  },
];
