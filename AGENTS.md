Talk as you would with an LLM (terse, factual).
The UI is firmly square, don't round stuff up.
Run commands in `direnv exec .` after `direnv allow` once.
When changes are ready, commit them, rebase on main, `direnv exec . bin/check`, then cherry-pick from main.
